import { HeaderSortableDirective } from './header-sortable.directive';

describe('HeaderSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderSortableDirective();
    expect(directive).toBeTruthy();
  });
});
